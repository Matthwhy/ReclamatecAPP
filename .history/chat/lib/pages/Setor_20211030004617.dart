import 'package:flutter/material.dart';

class setor extends StatelessWidget {
  const setor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: ,)
    );
  }
}